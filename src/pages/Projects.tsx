import { motion } from 'motion/react';
import { ArrowRight } from 'lucide-react';
import { projects } from '../data/projects';
import AnimatedButton from '../components/AnimatedButton';
import DetailsPanel from '../components/DetailsPanel';

export default function Projects() {
  return <div className="w-full flex flex-col items-center pb-40"><section className="w-full pt-48 pb-28 px-6 md:px-12 text-center"><motion.div initial={{opacity:0,y:30}} animate={{opacity:1,y:0}} className="max-w-4xl mx-auto"><span className="text-[var(--neon-cyan)] text-sm font-semibold tracking-widest uppercase mb-6 block">Selected Work</span><h1 className="text-5xl md:text-7xl font-bold text-white mb-8">Digital Experiences<br/><span className="text-transparent bg-clip-text bg-[var(--neon-gradient)]">Built to Perform.</span></h1><p className="text-xl text-[var(--text-secondary)] max-w-2xl mx-auto">A curated collection of digital products, commerce systems and interactive experiences.</p></motion.div></section><section className="w-full max-w-7xl px-6 md:px-12"><div className="grid grid-cols-1 md:grid-cols-2 gap-10 md:gap-16">{projects.map((project,idx)=><motion.article key={project.id} initial={{opacity:0,y:35}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-80px'}} transition={{duration:.6}} className={`group flex flex-col ${idx%2?'md:mt-20':''}`}><div className="relative aspect-[4/3] rounded-3xl overflow-hidden glass-card"><img src={project.image} alt={project.title} loading="lazy" className="w-full h-full object-cover opacity-75 group-hover:opacity-95 group-hover:scale-105 transition-all duration-700"/><div className="absolute inset-0 bg-gradient-to-t from-[#030508]/80 via-transparent to-transparent"/></div><div className="pt-6"><div className="flex justify-between text-xs tracking-widest uppercase"><span className="text-[var(--neon-cyan)]">{project.category}</span><span className="text-[var(--text-muted)]">{project.id}</span></div><h3 className="text-3xl font-bold text-white mt-3 group-hover:text-[var(--neon-cyan)] transition-colors">{project.title}</h3><p className="text-[var(--text-secondary)] leading-relaxed mt-3">{project.description}</p><div className="flex flex-wrap gap-2 mt-5">{project.technologies.map(t=><span key={t} className="px-3 py-1 rounded-full border border-[var(--border)] bg-[var(--surface-light)] text-xs text-[var(--text-secondary)]">{t}</span>)}</div><DetailsPanel><div className="space-y-5"><p className="leading-relaxed">This project combines focused product thinking, interface design and technical implementation around the needs of its users. The goal is a polished experience that remains fast, maintainable and scalable.</p><div><h4 className="text-white font-semibold mb-3">Technology stack</h4><p>{project.technologies.join(' • ')}</p></div><div><h4 className="text-white font-semibold mb-3">Project focus</h4><p>Performance, responsive interaction, clear information hierarchy and a refined visual system tailored to the {project.category.toLowerCase()} experience.</p></div><AnimatedButton to="/contact" variant="outline">Discuss Similar Project</AnimatedButton></div></DetailsPanel></div></motion.article>)}</div><div className="mt-24 flex justify-center"><AnimatedButton to="/contact" variant="outline">Discuss a Project</AnimatedButton></div></section></div>;
}
